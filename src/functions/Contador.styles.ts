import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    contador: {
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
        flexDirection: 'row'
    },

    texto: {
        fontSize: 40,
        fontFamily: 'Daruma',
        width: 50,
        height: 100,
        textAlign: 'center'
    }
})

